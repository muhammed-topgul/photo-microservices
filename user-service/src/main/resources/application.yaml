server:
  port: ${PORT:0}

spring:
  application:
    name: user-service
  config:
    import: optional:configserver:http://localhost:8888

logging:
  level:
    com:
      mtopgul:
        photoapp:
          userservice:
            service:
              AlbumServiceClient: DEBUG